# JOURNAL — MicroMatch

> Dated log of decisions, pivots, incidents, and quotes. Add entries as
> things happen — retrospectives need this raw material to land.
> Reverse-chronological; one paragraph max per entry.

## 2026-08-22 — Lottie scenes now use the SVG-only light player #decision

The dotLottie web component is deprecated and its final release still added direct `eval()` warnings to production builds. The app now extracts the embedded JSON from its local animation bundles and renders it with `lottie-web`'s SVG-only light player. It still loads on the client, respects reduced motion, and keeps the same static fallbacks, but leaves out the expression engine that contains `eval()`. The legacy dotLottie dependency and bundles are gone; the production client bundle contains no direct `eval()` calls.

## 2026-08-22 — The NGO calculator now makes a posting plan #decision

Reworked the NGO capacity calculator around work the organization can actually plan. It now turns a chosen backlog, task size, and delivery window into a mission count, daily posting cadence, and explicit review-time estimate. The volunteer-count and turnaround predictions are gone because the page has no data to support them. Review time is labeled as a three-minute-per-submission assumption, and the output uses a small stack of mission cards to connect the number to MicroMatch's claim-and-review workflow.

## 2026-08-22 — Public landing scenes play once and respect motion preferences #decision

Replaced the fictional profile and completion-count hero panels on the volunteer and NGO landing pages with three local dotLottie scenes. The volunteer scene pairs with a concrete sample task. The NGO scene shows the real brief, submission, and review sequence. The homepage impact scene replaces its confetti. A shared player loads only after its scene is visible, plays once, and shows a static fallback for reduced motion or a failed player import. The app already had the dotLottie component dependency, so this change adds no package.

## 2026-08-22 — Translation no longer blocks task-page navigation #decision

Moved the LibreTranslate call out of the task page load and into a task-scoped server endpoint. Selecting a language now updates the URL and leaves the original task page available while the browser requests translated display fields. The task detail panel shows a localized translating state and skeleton instead of making the entire page wait. The endpoint accepts only a supported language and loads the task itself, so the LibreTranslate key and arbitrary user-supplied text never reach the browser. Its response is privately cacheable for fifteen minutes; the existing successful-translation cache still applies on the server.

## 2026-08-22 — Task detail translation covers the whole task context #decision

Expanded `?lang=` on task pages beyond the title and full description. The server now translates the short description and displayed tag labels in the same LibreTranslate request, while keeping the NGO name unchanged as a proper name. The page also translates task-detail UI copy such as “Posted by,” translation notices, and claim prompts. LibreTranslate runs with one worker on the Oracle VM after the default worker count exhausted the VM during longer requests, and the application timeout is thirty seconds so slow but successful translations do not fall back early. Successful text is still cached individually; a provider failure leaves the page usable with original copy. Staging was checked with French and Portuguese task pages.

## 2026-08-21 — LibreTranslate now has a production boundary #decision

Replaced the legacy Azure-named translation helper with a server-only LibreTranslate client. Task detail pages still translate only the title and full description through `?lang=`, but requests now require the production API key, use a ten-second timeout, validate supported language codes, and cache successful results for fifteen minutes with a 500-entry limit. Provider failures return the original text without caching the failure. The deployment contract is `translate.micromatch.app` behind the Oracle VM's named Cloudflare Tunnel; `scripts/verify-libretranslate.ts` checks health, an authenticated translation, and rejection of a request without the key. The VM, DNS, and production secret rollout remain external deployment steps.

## 2026-08-21 — Rebuilt the MicroMatch videos in HyperFrames #milestone #decision

Replaced the Remotion projects with a vertical Tier 2 social cut in `ui-demo/` and a 16:9 Tier 3 trailer in `trailer/`. The social cut uses a 30 fps preconformed version of the original closed-loop recording so HyperFrames can seek it deterministically. The trailer is a native rebuild of the quarter-hour concept using the product’s real task and badge copy, without the untracked Blender laptop frames. Both 30 fps renders completed successfully, passed lint, had no detected silence, and passed visual review.

## 2026-08-07 — Ingested F-1 visa legal framework, sub-processor disclosures, and signup consent #decision #legal

Ingested policy framework for F-1 visa non-commercial portfolio compliance and third-party sub-processor disclosures. Updated `/privacy`and`/terms`with explicit disclosures for Vercel (hosting, request logs, edge network) and Appwrite (authentication, database, storage buckets, session cookies), non-commercial educational project disclaimers, "AS IS" limitation of liability, and data boundaries (user uploads stored on Appwrite, not used to train third-party AI models). Added signup form legal consent notice ("By registering, you agree to our Terms of Service and Privacy Policy") and documented legal positioning in`docs/positioning.md`.

## 2026-07-29 — The device shot is a script now, and STAIJA renders from the same one #decision #maintenance

Standardizing the three demo repos raised a question with a clear answer: STAIJA kept its phone as a committed `phone.blend`binary, we kept our laptop as`scratch/laptop.py`, and the script wins on every axis that matters. It diffs, it documents its own conventions in the docstring, and it takes the screen texture from a path, so re-screenshotting the site and re-running is the whole update loop instead of opening Blender and swapping an image by hand. Extracted the shared half into `tools/device-render/device.py`— render settings, slate body, emission screen, three-point lighting, tracked camera, sway loop — leaving`laptop.py`with geometry and framing only. Every laptop dimension and material value is byte-for-byte what it was; a test still on ampere-dev renders identically. Added`phone.py`alongside it and kept all three files identical with the STAIJA copy, which is what forced the env contract:`DEVICE_TEX`/`DEVICE_OUT`/`DEVICE_PREFIX` are required rather than defaulted, because a default prefix would be a repo-specific name inside a file that has to be shared. Only the README differs between the two repos. Worth knowing for the next port: the VM has no usable GPU backend, so Cycles falls back to CPU at roughly 145s/frame for the laptop, which makes a full 60-frame sequence about two and a half hours rather than the hour a Mac render takes.

## 2026-07-18 — The real Tier 3 is built from the name, not from a reference video #milestone #decision

Filled the `trailer/`slot the reclassification left open, and the storyboard came from a constraint that turned into the concept: fifteen minutes is a quarter of a clock face, so a coral arc that only ever draws 90° runs through the whole video — it ticks under the cold open, seals the match, rims the badges, and comes back for "Got fifteen minutes?". The center beat enacts the product's name: the Doctors Without Borders seed task slides in from the left, a "You, fifteen minutes before your next class" card from the right, and the wordmark assembles from the two halves when they click. Almost no site pixels anywhere; the one product appearance is the landing page inside the existing Blender laptop on the endcard, and the task stream re-uses the eight evergreen seed titles as styled cards rather than screenshots. Structure diffs clean against both STAIJA (no mission beat, no solution reveal, no stat rings — the arcs carry no numbers) and the ui-demo (no state-machine traversal, no footage). Music is MacLeod's "Inspired" with "Heartwarming" muxed as an audition alternate; the ukulele idiom stayed banned. One build bug worth remembering:`interpolate`throws on an`[Infinity, Infinity]` input range, so "no next line" needs a guard, not a sentinel. 53s, 1590 frames, deliberately text-plus-SFX with no voiceover unless one gets asked for.

## 2026-07-18 — The Tier 3 trailer is actually the Tier 2 UI demo #pivot #decision

Watched the trailer and the separately-built content-pipeline UI tour back to back and the tiers came out swapped. The trailer shows the product well enough that it reads as a UI demo, not as persuasion: it walks the feed, the claim, the submission, the approve, and the badge, all in real screens. STAIJA's Tier 3 is the contrast that made it obvious, since it sells the concept while barely showing the site. So `trailer/`is now the Tier 2 UI demo, and the Tier 3 slot is empty on purpose until a concept-first piece gets built in that register. The content-pipeline UI tour built the same day is dropped: it framed a cream-and-coral product inside a`#0d1117`box because it inherited the background from content-pipeline's shared card compositions, which is consistency with the tooling rather than with the brand. Music swapped a third time, Life of Riley out, Kevin MacLeod's "Wallpaper" in, chosen as a near-subliminal bed with no hook to fight narration;`public/CREDITS.md`and the README credit both now name it, and both were wrong before this (CREDITS said Life of Riley, README said Carefree, for a file that was Life of Riley). Renamed the same day to clear the naming debt:`trailer/`is now`ui-demo/`, the composition id is `MicroMatchUiDemo`, and the README, storyboard, package name, and credits all say demo. The name `trailer/`is left free on purpose so the real Tier 3 can take it. Also fixed a freeze the earlier verification pass missed: beat 6 opens on static dashboard footage and its push does not start until frame 80, so 1.7s of the beat had no motion at all.`FootageBeat`grew a`preDrift`prop, the mirror of the existing`pushCreep`, ramping a slow scale over the pre-push window. The freeze at the payoff ring was left alone, since a still beat at the climax is an edit rather than a bug.

## 2026-07-17 — The cursor dot was always 1.3× off target, and the glide exposed it #incident

Second round of trailer feedback: the dot now moves, but it presses buttons from a distance. Measured the offset in the frames and it came out to exactly the demo zoom factor: the dot lives inside the zoomed `<html>`, so its left/top get scaled by 1.3 while MouseEvent clientX/Y arrive in unzoomed viewport pixels, putting the dot 1.3× past every target. It had been wrong in every recording to date, including the shipped README GIFs; the teleport just never gave the eye time to line the dot up against a button. Fix is one division in the cursor init script (clientX / zoom), verified by frame-checking a probe recording with the dot sitting square on the ≤15 min chip. Same session also fixed the sidebar the recordings kept showcasing: brand row had zero horizontal padding while nav links had 16px, and the 40px logo box centered a 24px image, so nothing lined up. Brand padding now matches the nav inset, the logo box is sized to the image, and the sidebar got 20px side padding at 256px wide. Both fixes are app/demo-infra code, uncommitted; the trailer was re-recorded and re-rendered on top of them.

## 2026-07-17 — The demo cursor teleported because only transform had a transition #incident #decision

Feedback on the rendered trailer: the cursor dot jumps between click targets instead of moving. Two instant hops were stacked on each other. Playwright's click() places the pointer on the target in one move, and the injected dot's CSS only transitioned transform and background, so left and top snapped. Fixed in the demo helpers, not the trailer: the dot now transitions left/top over 380ms, and a demoClick() helper hovers before clicking, which lets the glide play out during the hover and slowMo's pause put the press at rest instead of mid-flight. Re-recording stretched the closed loop from 110s to 132s and moved every trim point, so the whole pinning pass ran again (reseed first, as always). Music swapped a second time: Wholesome out, Life of Riley in. Note for later: the Tier 1 README GIFs still show the teleporting cursor; a fresh `bun run demo` would regenerate all six with the glide.

## 2026-07-17 — Tier 3 trailer rebuilt from the product's own loop #milestone #decision

Rendered and verified the redesigned Tier 3 trailer: 50 seconds, eight beats, one task ("Translate a medical flyer into Spanish") followed from feed to badge. The spine is the Tier 1 closed-loop recording, not a storyboard borrowed from another video: the claim click, the submission, the NGO approve, and the First Mission unlock are all real product motion trimmed out of e2e/demo/output, which also meant no reseed, since nothing new was filmed. A mini task-card docked bottom-left collects a status chip as each stage completes (Claimed, In review, Approved, Badge minted), and the four chips form a ring at the end. Swapped the music from "Carefree" to Kevin MacLeod's "Wholesome" on feedback; credit line updated in trailer/public/CREDITS.md. Frame verification earned its keep again: the 1 fps pass looked fine, but the 3 fps pass across cuts caught half a second of empty cream at both the 38s and 43s transitions, fixed by starting each incoming scene inside the outgoing fade. Silence check clean, mix at −22.2 LUFS.

## 2026-07-16 — First trailer cut rejected as a palette swap #pivot #feedback

The v1 trailer reproduced STAIJA's scene structure beat for beat, same six components, same timeline, same motion curves, with MicroMatch's coral and copy dropped in. Verbatim: "I did say take inspiration. I'll concede on that. I did not, however, want a palette swap of the STAIJA video." Scene files and both renders deleted to force an original design; the STAIJA-free assets survive (the Blender laptop sequence, the authenticated screenshots, audio, configs). The redesign starts from MicroMatch's own shape, the claim-submit-approve-badge loop and the warm coral register, not from STAIJA's hook/mission/solution/climax arc. Reference material is for pacing and production quality, not for storyboards.

## 2026-07-16 — The trailer device is a laptop, and it had to be modeled from scratch #decision

Started the Tier 3 trailer in `trailer/` (own Remotion project, mirroring STAIJA's). The device shot is a laptop rather than STAIJA's phone, partly because no laptop Blender asset exists anywhere on this machine, but mostly because MicroMatch's best screens are desktop-shaped: the badge studio, the analytics page, the feed with its filter row. The build took three test stills to get right. First the camera was so close the deck filled the frame; then the lid turned out to be planted mid-deck like a monitor because I'd centered it at y=0 instead of the rear hinge; then the "dark slate metallic" body rendered near-white, since metal at 0.85 takes its color from whatever it reflects, and a 0.35 gray world is effectively a white dome. Darkening the world to 0.2 and roughing the deck to 0.52 fixed it. Sixty-frame ±35° sway loop at 2400×1600, cycled at half speed in Remotion, same as the phone pipeline. The climax rings say Claimed/Approved/Badged at 100% instead of quoting usage numbers, because there are no usage numbers yet and inventing them was off the table.

## 2026-07-16 — Puppeteer clicked the Google button because both forms say submit #incident

The screenshot run for the trailer logged in as the demo volunteer, or so it claimed: `page.click('button[type="submit"]')`on the login page actually hit the Google OAuth button, because the OAuth form sits above the email form and both end in a submit button. Half the "authenticated" captures came back as login redirects. The demo suite never hit this because Playwright's`getByRole('button', { name: /Sign in/i })`selects by accessible name, not by position. Scoping the selector to`form.auth-form button[type="submit"]`fixed the volunteer shots; the NGO login then stalled anyway until it got a fresh incognito context, since the Appwrite client session from the previous login survives cookie deletion. Lesson: on a page with two forms,`button[type=submit]` is a coin flip, and signing out is not the same as starting clean.

## 2026-07-16 — Watched the demo back and found the payoff shot below the fold #incident

The closed-loop recording passed every assertion, so it was ready to ship. Extracted frames to actually look at it and the final beat, the entire reason the scenario exists, had the earned badge tile sitting just under the viewport: the "Badge vault" heading and "1 earned" were in shot, the tile itself wasn't. `scrollIntoViewIfNeeded()`moves the minimum distance, so anchoring on the section heading parks that heading at the bottom edge and leaves everything below it off-screen.`toBeVisible()`stayed green throughout because the element was rendered and unobscured, which is all it actually checks. Swapped the anchor to the tile and the assertion to`toBeInViewport()`, which is the one that would have caught it, and did the same to the locked-vault shot at the top so the before/after framing matches. Lesson: for anything whose output is a video, the exit code isn't the test, the frames are.

## 2026-07-16 — Seeding grew two sign-in-able accounts, because the loop can't be filmed without them #decision

Recording claim → submit → approve → badge needs two accounts that can sign in through the real UI, and `scripts/seed.ts`only ever made one NGO user with a throwaway random password, under a comment reading "no one signs in as this account; we mutate via admin SDK only." Extended it to set a real password on that user, add a demo volunteer, and create a`task-completion`badge definition so approval has something to mint. The NGO has to be that seeded user specifically:`/api/claims/[id]/approve`403s any reviewer who doesn't own the task, so a separate reviewer account can't stand in. The password comes from`SEED_DEMO_PASSWORD`and the whole block no-ops when it's unset, because hardcoding it would commit an NGO-role login that can approve claims and mint badges, pointed at the same Appwrite project the live site runs on. The other half was making the seed reset the volunteer's claims and badges every run:`processBadgeAwards`skips any label the user already holds, so a second recording approves the claim and reveals no badge while every assertion still passes. Named the badge "First Mission" to match one of the locked placeholders already in the vault, so the approval visibly unlocks that exact tile. Wired`bun run seed`into`bun run demo` so nobody has to remember. Lesson: fixture state with a shelf life should be reset by the script that depends on it, not by a line in a README that someone reads once.

## 2026-07-16 — The demo login put the password in the URL when hydration lost the race #incident

Building the closed-loop spec, ran the login helper with slowMo at zero and watched Playwright navigate to `/login?email=demo%40micromatch.app&password=Demo-...`. The form had done a plain native GET: `on:submit={handleEmailSignIn}`is the thing that calls`preventDefault()`, and the click landed before Svelte hydrated, so nothing intercepted it. At the normal `DEMO_SLOWMO=1200`this never reproduces, since every action pauses over a second and hydration always wins, which is precisely why it could have sat there indefinitely. It only surfaced because the probe ran at zero pacing. Fixed by waiting on`<dotlottie-player>`, which AuthBrandPanel only renders after onMount's dynamic import resolves, so its presence proves hydration finished. The same bug was already sitting in 02-signup-flow, where clicking the role picker pre-hydration silently does nothing and the form never appears; it had gone unnoticed because slowMo hid it there too. Lesson: the pacing that makes a demo readable also papers over real races, so the honest place to run a demo spec is at zero slowMo, not at recording speed.

## 2026-07-16 — Demo tasks weren't deleted, they'd just aged out of the feed #incident

Pre-flighting the marketing/demo trailer shoot, checked the production site and found `/tasks`showing the "You're too fast!" empty state, the same one local dev shows with no seed data. Ran`bun run seed`expecting it to backfill, and it reported "8 already present": the demo tasks were still sitting in Appwrite the whole time. Something was hiding them from the feed, not deleting them. Traced it to`filterTasksForFeed()`in`appwrite.ts`, which auto-archives any task whose `lastActivityAt`is more than 30 days stale (a feature meant to hide dead listings). The 8 demo tasks were seeded 2026-05-09 and, being demo tasks, never got organic claims to keep`lastActivityAt`fresh, so they silently aged past the 30-day line with zero errors anywhere: not in Vercel runtime logs, not in the seed script's own output, since its idempotent "skip if title exists" path never touched the row again after creation. Fixed`ensureTasks()`to look existing rows up by id instead of just title, and to call`updateRow`on their`status`/`lastActivityAt` every run, so re-seeding revives stale demo data instead of being a no-op. Re-ran it and the 8 tasks are back live. Lesson: an idempotent "skip if exists" script and a silent auto-archive filter are each fine on their own, but stacked together they produce a bug with no error message and no code path that ever surfaces it. Seed data has a shelf life now, and this exact wall will get hit again before every future demo unless someone reseeds first.

## 2026-07-16 — CI had been red for two days on a compiler bump nobody was watching #incident

Checked whether CI needed updating after the auth fixes and found it had been failing since commit e4c2ed7 two days earlier: "bump dependencies to latest" had pulled `typescript`up to 7.0.2, the Go-ported rewrite, and both`Type-check`and`Lint`jobs were crashing before they even reached a file. svelte-check reaching into`typescript.sys`got`undefined`, and typescript-eslint's `typescript-estree`failed the same way. Confirmed via`gh run list`that the two most recent pushes to main both show`failure`, and the logs matched a local repro exactly. The real tell was in typescript-eslint's own package metadata: its published peerDependency range is `typescript: ">=4.8.4 <6.1.0"`, so TS7 was never going to work no matter which version got installed. Pinned back to `^5.9.3`, the newest release inside that window, and both jobs went green: svelte-check at 0 errors, eslint at 0 errors and 140 pre-existing warnings that don't fail the build. One unrelated find along the way: eslint never had `.vercel/**`or`coverage/**` in its ignore list, invisible in CI since both are gitignored, but a wall of noise for anyone linting locally after a build. Added them. Lesson: "bump dependencies to latest" is not a decision, it's a coin flip against whatever the rest of the ecosystem has caught up to.

## 2026-07-16 — hooks.server.ts had a fallback that let anyone become an NGO for free #incident

A security review found that `hooks.server.ts`fell back to the client-writable`mm_role`cookie to populate`event.locals.userRole`whenever the HttpOnly`mm_session`cookie was missing or expired, and`getUserRole()`treats`locals.userRole`as authoritative. That meant`Cookie: mm_role=ngo`, sent with zero authentication, was enough to pass every `role === 'ngo'`gate: approve or reject any claim, run`/api/tasks/maintenance`(which sweeps every org's tasks, not just the caller's), claim tasks anonymously. Most NGO-only routes survived anyway because they also require`session.user.id`for ownership lookups, and the forged cookie never populates that. Claims approve/reject didn't do an ownership check at all, though, so two real logged-in NGOs could already approve or reject each other's claims and mint badges on tasks they don't own; the cookie bug just made the same holes reachable by logged-out attackers too. Fixed by deriving`locals.userRole`only from a validated`mm_session`lookup or`getUserRole()`'s JWT/token fallback, adding task-ownership checks to both claim review endpoints, and moving `/api/tasks/maintenance`from "any NGO" to`isUserAdmin`. `mm_role` still exists and is still read, just client-side now, in Sidebar.svelte and +layout.svelte, for optimistic rendering before the session round-trip lands. Lesson: a code comment promising a cookie is "not used for authorization" is a bug report waiting to happen if nothing actually enforces it.

## 2026-05-12 — Wrote a positioning doc instead of pretending we're Idealist #decision

Added `docs/positioning.md`to pin down what MicroMatch actually is, because the README kept drifting toward generic "volunteer platform" language. The honest framing: Idealist is a directory you leave to email an org; MicroMatch is a marketplace where the work happens on-platform — claim → submit → approve → badge is one closed loop. The doc names the tradeoff out loud rather than hiding it: Idealist's moat is two decades of supply density and SEO, and our proof-review loop is real NGO friction (posting on Idealist costs an email address; posting here costs grading time). The bet is that bite-sized + verified + badge-bearing is a different enough product that the supply-side friction is worth it. Same day, deleted the stale`PLAN.md` — it was describing a project that no longer existed.

## 2026-05-09 — The Safari ITP OAuth loop that took five commits to kill #incident

Google sign-in was sending Safari users into an infinite loop: OAuth → /profile → save → /login → OAuth, forever. The root cause was Intelligent Tracking Prevention stripping the session cookie set by `cloud.appwrite.io`as third-party, so every`account.*`call returned 401. We thrashed through it — first bounced to /login on a load-time 401 (broke Google OAuth entirely, since the cookie hadn't propagated by onMount), then moved it to save-time, then gave up auto-redirecting and showed an amber "try another browser" banner. The actual fix was architectural: switch from`createOAuth2Session`to`createOAuth2Token`(RFC 6749 code exchange), add a server endpoint that validates`userId`+`secret`with the admin SDK and mints a first-party`mm_session` cookie, and move profile saves server-side too. Lesson: a cross-origin session cookie was never going to survive ITP — the client SDK had to stop being the auth boundary.

## 2026-05-09 — Seeded tasks weren't showing on prod because two env vars were undocumented #incident

The deployed feed was empty even though eight demo tasks lived in Appwrite Cloud. Turned out `APPWRITE_DB_ID`and`APPWRITE_TASKS_TABLE_ID`were never in`.env.example`, so `getTasks()` silently fell through to the in-memory task store — which is empty after every cold start on Vercel. Documented them (`micromatch`/`tasks`from`appwrite.config.json`) and wrote `scripts/seed.ts` to be idempotent by construction: users matched by email, prefs read-modify-write so other keys survive, membership swallows 409, tasks skipped if the title already exists for the org. The seed tasks deliberately carry no deadlines and no relative copy ("this Friday") so the landing page never goes stale.

## 2026-05-08 — Vitest bootstrap broke Vercel deploys via a codepath local builds never hit #incident

Adding Vitest perturbed the dependency graph just enough that `adapter-auto`'s lazy detect-and-import for Vercel resolved a stale `estree-walker`with no`"."`export, failing every deploy with`No "exports" main defined`. Local `bun run build`stayed green the whole time because it skips Vercel detection entirely — so the break was invisible until it hit CI. Fix: drop`adapter-auto`for`adapter-vercel`directly (resolution at install time, not build time) and pin`nodejs22.x`in`svelte.config.js`, because Bun's compat layer reports Node v25.x and adapter-vercel rejects that without an explicit runtime. The takeaway: "works locally" means nothing when the failing codepath only fires under the host's environment detection.

## 2026-05-08 — Test suite caught a real millisecond-collision bug in the dev fallback #milestone #incident

Bootstrapped Vitest and wrote coverage for everything shipped over the prior weeks — tagColors, propublica, email, verifications, badgeDefs, badgeCriteria, badgeAwarder. The suite immediately earned its keep: the in-memory IDs in `verifications.ts`and`badgeDefs.ts`were generated as`mem-${Date.now()}`and collided when two rows were created in the same millisecond, silently overwriting each other. Only affects the Appwrite-less dev fallback, but it was a genuine correctness bug a human would never have noticed by clicking around. Replaced with a counter-suffixed generator. Later the component tests forced migrating to Vitest's`projects`config —`@testing-library/svelte`kept resolving Svelte 5's SSR entry instead of the client one, throwing`lifecycle_function_unavailable`, until each project got the module resolution it needed (node for server, jsdom + `browser` conditions for components). Landed at 123 tests, svelte-check at 0 errors.

## 2026-05-08 — Verification is a soft gate, and clients can't claim it anymore #decision

Built the NGO verification flow as a *soft*gate: NGOs submit org name, country, tax ID, and an optional doc; admins (gated by Appwrite Teams membership) review a queue enriched with ProPublica 501(c)(3) lookups so they can eyeball the EIN against the claimed org name. The non-obvious call: tasks now derive `isVerified` server-side from the verification record, so a client can no longer mark its own tasks verified. Approval back-fills the Verified chip onto every existing task the NGO owns; rejection clears it; both email the NGO via Mailgun's HTTP API (no SDK dependency). Same batch moved badge definitions out of a hardcoded array into an org-owned Appwrite table, and shifted badge awarding from claim*creation*to claim*approval* — because that's when the work is actually verified done — deduped by label so the same badge can't be minted twice.

## 2026-05-08 — Ripped out the Azure HelpBot; it was scaffolding that never earned its slot #pivot

Removed the Azure Direct Line HelpBot widget entirely. `/api/bot/token`now returns 410 Gone for any straggler caller, and the auth pages stopped defaulting to the bot's`help.lottie`mascot. Same cleanup killed an unconditional`account.get()`in the layout's onMount that was firing a 401 in the console for every signed-out visitor — now gated behind a session hint (the`mm_role`cookie or`mm_has_session`flag). A few days earlier we'd also dropped`@auth/core`and`@auth/sveltekit`: they were scaffolded in but never wired up — the real auth flow runs through Appwrite directly. Pattern across this stretch: delete the half-integrated dependencies rather than carry them as dead weight.

## 2026-05-04 — Migrated Databases → TablesDB and renamed every collection constant #decision

Bumped `node-appwrite`to v24 and migrated the whole server layer from Appwrite's Databases API to the newer TablesDB API, renaming every`*_COL_ID`env var to`*_TABLE_ID`. Not a feature, but a foundational call that touched all the data-access code — worth doing before piling verification, badges, and roles on top of it. Around the same time the brand redesign carried the warm landing-page language through to every authenticated screen (dashboards, feed, TaskCard, auth pages), with a Plus Jakarta Sans + coral system in `app.css` and a new logo that doubles as the favicon.

## 2026-04-08 — Revived the project after ~8 months, swapped the sterile blue SaaS look for a warm consumer aesthetic #pivot

Picked MicroMatch back up after roughly eight dormant months and immediately rebuilt the homepage from a Figma design — out went the blue SaaS landing page, in came cream backgrounds, coral CTAs, Plus Jakarta Sans headings, floating hero mockup cards, and a gamification showcase with a progress ring. The Layout TopAppBar is now hidden on the home route so the landing page renders its own sticky header with anchor nav. This also marked the tooling pivot: npm → Bun, with a Tailwind toolchain and a Lottie player added. The redesign set the visual direction that everything else in the May sprint had to live up to.

## 2025-08-14 — MVP in a week: browse → pick → learn → complete → earn #milestone

Shipped the first working MVP three days after the initial commit, framed around a five-verb loop: Browse → Pick → Learn → Complete → Earn. The original pitch leaned hard on just-in-time learning — each task was meant to carry DataCamp and Educative "learning cards" so a volunteer new to a skill could still contribute — plus auto-translation of task copy to the viewer's language and a gamified XP-bar-plus-badges dashboard. The planned deployment was Heroku for the SvelteKit SSR app and DigitalOcean for Appwrite. Most of that early scope (Heroku, DigitalOcean, the learning cards) didn't survive the 2026 revival; the closed claim/approve/badge loop and Appwrite backend did.
